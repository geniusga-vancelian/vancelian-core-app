/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Dev-only frontend - no production optimizations needed
  output: 'standalone',
}

module.exports = nextConfig

